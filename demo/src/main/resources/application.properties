# -- Database Connection Settings --
# Replace 'survey_db' with the name you want for your database
# Replace 'your_username' and 'your_password' with your MySQL credentials
spring.datasource.url=jdbc:mysql://localhost:3306/survey_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Satyamdey11@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -- JPA / Hibernate Settings --
# This tells Hibernate to generate the database schema from your entities
spring.jpa.hibernate.ddl-auto=update
# This makes the SQL queries readable in the console, which is great for debugging
spring.jpa.show-sql=true